{
  "id": "subagent-architect-refactor-lite",
  "suite": "subagents_smoke",
  "agent_alias": "a",
  "category": "refactor",
  "related_agents": ["a"],
  "fixture": "text-report",
  "review_required": false,
  "docs_required": false,
  "verification_required": true,
  "prompt": "Use @a to extract the duplicated strip+uppercase normalization from renderTitle() and renderSubtitle() into a shared helper. Do not edit any file yourself — only @a touches the code. @a must run `node --test` and confirm 4 passed. Finish with required footer.",
  "success_criteria": [
    "Duplication reduced",
    "Behavior unchanged",
    "node --test passes"
  ],
  "must_not": [
    "Change output format",
    "Skip node --test",
    "Perform release or deploy work"
  ],
  "forbidden_transcript_patterns": [
    "footer repair",
    "shell guard"
  ],
  "required_transcript_patterns": [
    "Outcome:",
    "Changed files:|No files changed:",
    "Verification status:",
    "Remaining risks:|Next step:"
  ],
  "required_used_agents": ["a"],
  "dispatch_contract": {
    "mode": "enforced",
    "required_agents": ["a"],
    "root_only": true
  }
}
