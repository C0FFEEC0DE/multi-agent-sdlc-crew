name: Repository Checks

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  repository-checks:
    name: Repository Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.23'

      - name: Install workflow lint tools
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck
          go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.7
          echo "$HOME/go/bin" >> "$GITHUB_PATH"

      - name: Run repository validator
        run: bash scripts/validate.sh

      - name: Check shell syntax
        run: bash -n install.sh claudecfg/install.sh claudecfg/hooks/*.sh scripts/*.sh

      - name: Check key JSON files
        run: |
          jq empty claudecfg/settings.json
          jq empty tests/hooks/cases.json
          jq empty tests/hooks/scenarios.json
          find bench/tasks -type f -name '*.json' -exec jq empty {} +
          jq empty bench/schema/*.json

      - name: Check whitespace errors
        run: git diff --check
