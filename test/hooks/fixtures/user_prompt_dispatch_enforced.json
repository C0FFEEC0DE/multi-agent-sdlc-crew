{
  "session_id": "scenario-dispatch-enforced",
  "prompt": "BENCHMARK_DISPATCH_CONTRACT: root_only; mode=enforced; roles=a\nUse @a to extract the duplicated strip+uppercase normalization from render_title() and render_subtitle() into a shared helper. Do not edit any file yourself — only @a touches the code. @a must run pytest -q and confirm 4 passed. Finish with required footer."
}